import java.util.ArrayList;import java.util.List;import java.util.Scanner; public class A_Line_Trip {    public static void main(String[] args) {        Scanner sc = new Scanner(System.in);        int T = sc.nextInt();         while (T-- > 0) {            int n = sc.nextInt();            int x = sc.nextInt();            List<Integer> points = new ArrayList<>();            points.add(0);            for (int i = 0; i < n; i++) {                points.add(sc.nextInt());            }            points.add(x);            n = points.size();            int max = Integer.MIN_VALUE;            for (int i = 1; i < n; i++) {                if (i == n - 1) {                    max = Math.max(max, 2 * (points.get(i) - points.get(i - 1)));                } else {                    max = Math.max(max, points.get(i) - points.get(i - 1));                }            }             System.out.println(max);        }         sc.close();    }}