import java.util.Scanner; public class A_Don_t_Try_to_Count {     public static void main(String[] args) {        Scanner sc = new Scanner(System.in);        int T = sc.nextInt();         while (T-- > 0) {            int n = sc.nextInt();            int m = sc.nextInt();            String x = sc.next();            String s = sc.next();             String x1 = x + x;            String x2 = x1 + x1;            String x3 = x2 + x2;            String x4 = x3 + x3;            String x5 = x4 + x4;            int ans = -1;            if (check(s, x)) {                ans = 0;            } else if (check(s, x1)) {                ans = 1;            } else if (check(s, x2)) {                ans = 2;            } else if (check(s, x3)) {                ans = 3;            } else if (check(s, x4)) {                ans = 4;            } else if (check(s, x5)) {                ans = 5;            }             System.out.println(ans);         }        sc.close();    }     private static boolean check(String s, String x) {         if (x.length() < s.length()) {            return false;        }        // if (x.contains(s))        // return true;        for (int i = 0; i <= x.length() - s.length(); i++) { // n - m + 1            // Check if the substring of x starting at i with length of s is equal to s            if (x.substring(i, i + s.length()).equals(s)) { // m                return true; // s is a substring of x            }        }        return false; // s is not a substring of x    } // O(n*m) }