import java.util.*;import java.io.*; public class Main {    public static void main(String[] args) throws IOException {        FastReader fr = new FastReader();        int t = fr.nextInt();         while (t-- > 0) {            int n = fr.nextInt();            int k = fr.nextInt();            int range = n * k;            int div = n / 2;            long sum = 0;            int j = 0;             long[] v = new long[range];            for (int i = 0; i < range; i++) {                v[i] = fr.nextLong();            }             for (int i = range - 1 - div; i >= 0; i -= div + 1) {                sum += v[i];                j++;                if (j == k) break;            }             System.out.println(sum);        }    }     // Fast input reader (similar to scanf/fastread in C++)    static class FastReader {        BufferedReader br;        StringTokenizer st;         FastReader() {            br = new BufferedReader(new InputStreamReader(System.in));        }         String next() throws IOException {            while (st == null || !st.hasMoreElements()) {                st = new StringTokenizer(br.readLine());            }            return st.nextToken();        }         int nextInt() throws IOException {            return Integer.parseInt(next());        }         long nextLong() throws IOException {            return Long.parseLong(next());        }    }}