import java.util.Scanner; public class Main {    public static void main(String[] args) {        Scanner sc = new Scanner(System.in);         int t = sc.nextInt(); // number of test cases        while (t-- > 0) {            long a = sc.nextLong();            long b = sc.nextLong();             long res = 1 + a / b; // initial upper bound on operations             for (long p = b; p <= b + 100; p++) {                if (p == 1) continue;                 long div = 0, cur = a;                while (cur > 0) {                    cur /= p;                    div++;                }                 long cnt = (p - b) + div; // increments + divisions                res = Math.min(res, cnt);            }             System.out.println(res);        }        sc.close();    }}