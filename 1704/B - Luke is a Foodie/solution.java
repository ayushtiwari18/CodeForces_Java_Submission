import java.util.Scanner; public class Main {    public static void main(String[] args) {        Scanner sc = new Scanner(System.in);                long t = sc.nextLong(); // number of test cases                while (t-- > 0) {            long n = sc.nextLong();            long x = sc.nextLong();                        long cnt = 0;            long low = 0;            long high = (long)2e9 + 7;  // initialize with large value                        for (long p = 0; p < n; p++) {                long cur = sc.nextLong();                                long left = cur - x;                long right = cur + x;                                // Update low and high                low = Math.max(low, left);                high = Math.min(high, right);                                // If invalid interval, reset and count                if (low > high) {                    cnt++;                    low = left;                    high = right;                }            }                        System.out.println(cnt);        }                sc.close();    }}