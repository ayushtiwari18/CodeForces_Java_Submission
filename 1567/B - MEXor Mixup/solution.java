import java.util.Scanner; public class Main {     // Compute XOR of all numbers from 1 to n    static long computeXOR(long n) {        if (n % 4 == 0) return n;        else if (n % 4 == 1) return 1;        else if (n % 4 == 2) return n + 1;        return 0; // n % 4 == 3    }     public static void main(String[] args) {        Scanner sc = new Scanner(System.in);         long t = sc.nextLong(); // number of test cases        while (t-- > 0) {            long a = sc.nextLong();            long b = sc.nextLong();             long x = computeXOR(a - 1);            long res = a + 2;             if (x == b) {                res = a;            } else if ((x ^ b) != a) { // ^ is bitwise XOR in Java                res = a + 1;            }             System.out.println(res);        }        sc.close();    }}