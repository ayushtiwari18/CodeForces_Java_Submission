import java.util.*; public class A_Forked {     static int[] dx = { -1, 1, -1, 1 };    static int[] dy = { -1, -1, 1, 1 };     static class Pair {        int x;        int y;         Pair(int x, int y) {            this.x = x;            this.y = y;        }         @Override        public boolean equals(Object o) {            if (this == o)                return true;            if (o == null || getClass() != o.getClass())                return false;            Pair pair = (Pair) o;            return x == pair.x && y == pair.y;        }         @Override        public int hashCode() {            return Objects.hash(x, y);        }    }     public static void main(String[] args) {        Scanner sc = new Scanner(System.in);        int T = sc.nextInt();        while (T-- > 0) {            int a = sc.nextInt();            int b = sc.nextInt();            int kingX = sc.nextInt();            int kingY = sc.nextInt();            int queenX = sc.nextInt();            int queenY = sc.nextInt();             Set<Pair> king_hits = new HashSet<>();            Set<Pair> queen_hits = new HashSet<>();             for (int i = 0; i < 4; i++) {                king_hits.add(new Pair(kingX + dx[i] * a, kingY + dy[i] * b));                king_hits.add(new Pair(kingX + dx[i] * b, kingY + dy[i] * a));                 queen_hits.add(new Pair(queenX + dx[i] * a, queenY + dy[i] * b));                queen_hits.add(new Pair(queenX + dx[i] * b, queenY + dy[i] * a));            }             int ans = 0;            for (Pair pos : king_hits) {                if (queen_hits.contains(pos)) {                    ans++;                }            }             System.out.println(ans);         }        sc.close();    }}