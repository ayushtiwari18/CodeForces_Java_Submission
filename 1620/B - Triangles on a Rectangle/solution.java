import java.util.*; public class Main {    public static void main(String[] args) {        Scanner sc = new Scanner(System.in);        int t = sc.nextInt();              // number of test cases         while (t-- > 0) {            long w = sc.nextLong();        // width of the big rectangle            long h = sc.nextLong();        // height of the big rectangle             // --- compute maximum horizontal width ---            long width = 0;             int k = sc.nextInt();            long first = sc.nextLong();            for (int i = 1; i < k; i++) {                long val = sc.nextLong();                width = val - first;       // last-first for this side            }             k = sc.nextInt();            first = sc.nextLong();            for (int i = 1; i < k; i++) {                long val = sc.nextLong();                long cur = val - first;                if (cur > width) width = cur;            }             long res = width * h;          // candidate area from horizontal slices             // --- compute maximum vertical height ---            long height = 0;             k = sc.nextInt();            first = sc.nextLong();            for (int i = 1; i < k; i++) {                long val = sc.nextLong();                height = val - first;            }             k = sc.nextInt();            first = sc.nextLong();            for (int i = 1; i < k; i++) {                long val = sc.nextLong();                long cur = val - first;                if (cur > height) height = cur;            }             res = Math.max(res, height * w);             System.out.println(res);        }        sc.close();    }}