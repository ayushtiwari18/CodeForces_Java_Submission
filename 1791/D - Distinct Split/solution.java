import java.util.*; public class Main {    public static void main(String[] args) {        Scanner sc = new Scanner(System.in);        int t = sc.nextInt();                while (t-- > 0) {            int n = sc.nextInt();            String s = sc.next();                        int[] left = new int[n];            int[] right = new int[n];                        boolean[] seen = new boolean[26];            int cnt = 0;                        // left array: distinct chars from left to right            for (int i = 0; i < n; i++) {                int cur = s.charAt(i) - 'a';                if (!seen[cur]) {                    cnt++;                    seen[cur] = true;                }                left[i] = cnt;            }                        // reset for right            Arrays.fill(seen, false);            cnt = 0;                        // right array: distinct chars from right to left            for (int i = n - 1; i >= 0; i--) {                int cur = s.charAt(i) - 'a';                if (!seen[cur]) {                    cnt++;                    seen[cur] = true;                }                right[i] = cnt;            }                        int res = 0;            for (int i = 1; i < n; i++) {                int test = left[i - 1] + right[i];                res = Math.max(res, test);            }                        System.out.println(res);        }                sc.close();    }}