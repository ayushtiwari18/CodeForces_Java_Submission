import java.util.*; public class A_Doremy_s_Paint_3 {    public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);        int t = scanner.nextInt();         while (t-- > 0) {            int n = scanner.nextInt();            long[] a = new long[n];             for (int i = 0; i < n; i++) {                a[i] = scanner.nextLong();            }             Map<Long, Integer> freq = new HashMap<>();            for (long x : a) {                freq.put(x, freq.getOrDefault(x, 0) + 1);            }             if (freq.size() > 2) {                System.out.println("No");                continue;            }             if (freq.size() == 1) {                System.out.println("Yes");                continue;            }             // Exactly two distinct elements            Iterator<Integer> it = freq.values().iterator();            int f1 = it.next();            int f2 = it.next();             if (n % 2 == 0) {                System.out.println(f1 == f2 ? "Yes" : "No");            } else {                System.out.println(Math.abs(f1 - f2) == 1 ? "Yes" : "No");            }        }        scanner.close();    }}