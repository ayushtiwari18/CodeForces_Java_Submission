import java.util.*; public class Main {    static int roundUp(int a, int b) {        if (a % b == 0) return a / b;        return a / b + 1;    }     public static void main(String[] args) {        Scanner sc = new Scanner(System.in);        int t = sc.nextInt();                while (t-- > 0) {            int n = sc.nextInt();            int k = sc.nextInt();            int[] arr = new int[n];            int evenCount = 0;                        for (int i = 0; i < n; i++) {                arr[i] = sc.nextInt();                if (arr[i] % 2 == 0) evenCount++;            }                        int ans = Integer.MAX_VALUE;            for (int i = 0; i < n; i++) {                int need = roundUp(arr[i], k) * k - arr[i];                ans = Math.min(ans, need);            }                        // Special case for k = 4            if (k == 4) {                if (evenCount >= 2) ans = 0;                else if (evenCount == 1) ans = Math.min(ans, 1);                else ans = Math.min(ans, 2);            }                        System.out.println(ans);        }    }}