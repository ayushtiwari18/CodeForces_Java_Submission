import java.util.*; public class Main {    public static void main(String[] args) {        Scanner sc = new Scanner(System.in);        int t = sc.nextInt();         while (t-- > 0) {            int n = sc.nextInt();            long[] v = new long[n + 1];            for (int i = 0; i < n; i++) {                v[i] = sc.nextLong();            }            v[n] = 1000000007L;  // sentinel             int sz = 1, start = 0;            boolean possible = true;            List<Long> w = new ArrayList<>();             for (int p = 1; p <= n; p++) {                if (v[p] == v[p - 1]) {                    sz++;                    continue;                }                if (sz == 1) {                    possible = false;                    break;                }                // push last element of the block                w.add((long) p);                // push the rest of the block                for (int u = start; u < p - 1; u++) {                    w.add((long) (u + 1));                }                start = p;                sz = 1;            }             if (possible) {                for (long val : w) {                    System.out.print(val + " ");                }                System.out.println();            } else {                System.out.println(-1);            }        }        sc.close();    }}