import java.util.Scanner; public class Main {    public static void main(String[] args) {        Scanner sc = new Scanner(System.in);         long t = sc.nextLong(); // number of test cases        while (t-- > 0) {            long n = sc.nextLong();            long m = sc.nextLong();             long mn = Long.MAX_VALUE; // minimum absolute value            long cnt = 0;             // count of negatives            long res = 0;             // total sum             for (long row = 0; row < n; row++) {                for (long col = 0; col < m; col++) {                    long x = sc.nextLong();                    if (x < 0) {                        x = -x;                        cnt++;                    }                    mn = Math.min(mn, x);                    res += x;                }            }             // If odd count of negatives, subtract twice the minimum            res -= 2 * (cnt % 2) * mn;            System.out.println(res);        }         sc.close();    }}