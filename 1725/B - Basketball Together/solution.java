import java.util.*; public class Main {    public static void main(String[] args) {        Scanner sc = new Scanner(System.in);         long n = sc.nextLong();        long d = sc.nextLong();         Long[] a = new Long[(int)n];        for (int i = 0; i < n; i++) {            a[i] = sc.nextLong();        }         // Sort in descending order        Arrays.sort(a, Collections.reverseOrder());         long rem = n;        long ans = 0;         for (int i = 0; i < n; i++) {            long x = a[i];            long num = 1 + d / x;             if (num <= rem) {                rem -= num;                ans++;            } else {                break;            }        }         System.out.println(ans);    }}