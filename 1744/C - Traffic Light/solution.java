import java.util.*; public class Main {    public static void main(String[] args) {        Scanner sc = new Scanner(System.in);                int t = sc.nextInt();        while (t-- > 0) {            int n = sc.nextInt();            char c = sc.next().charAt(0);            String s = sc.next();             if (c == 'g') {                System.out.println(0);                continue;            }             int nxt = 0;            // Find first 'g' to handle wrap-around            for (int p = 0; p < n; p++) {                if (s.charAt(p) == 'g') {                    nxt = n + p;                    break;                }            }             int res = 0;            for (int p = n - 1; p >= 0; p--) {                if (s.charAt(p) == 'g') {                    nxt = p;                } else if (s.charAt(p) == c) {                    int cur = nxt - p;                    res = Math.max(res, cur);                }            }             System.out.println(res);        }        sc.close();    }}