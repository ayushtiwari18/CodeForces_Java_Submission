import java.util.*; public class Main {        static class Pair {        long rem;        long idx;        Pair(long rem, long idx) {            this.rem = rem;            this.idx = idx;        }    }        static class PairComparator implements Comparator<Pair> {        @Override        public int compare(Pair a, Pair b) {            if (a.rem == b.rem) {                return Long.compare(a.idx, b.idx); // ascending by index            } else if (a.rem == 0) {                return -1; // a comes first            } else if (b.rem == 0) {                return 1; // b comes first            } else {                return Long.compare(b.rem, a.rem); // descending by remainder            }        }    }        public static void main(String[] args) {        Scanner sc = new Scanner(System.in);        int t = sc.nextInt();                while (t-- > 0) {            int n = sc.nextInt();            long k = sc.nextLong();                        List<Pair> list = new ArrayList<>();            for (int i = 0; i < n; i++) {                long x = sc.nextLong();                list.add(new Pair(x % k, i + 1));            }                        Collections.sort(list, new PairComparator());                        for (Pair p : list) {                System.out.print(p.idx + " ");            }            System.out.println();        }                sc.close();    }}